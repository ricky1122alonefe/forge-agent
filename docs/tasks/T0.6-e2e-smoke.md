# T0.6 — 端到端冒烟测试

| 字段 | 值 |
|---|---|
| **任务 ID** | T0.6 |
| **所属 Phase** | Phase 0 |
| **估时** | 1 小时 |
| **依赖** | T0.1, T0.2, T0.3 |
| **必须性** | 🔴 阻塞 |
| **状态** | ⬜ 未开始 |
| **自动化验证** | `bash scripts/smoke_test.sh` |
| **最近验证** | — |

---

## 目标

完整跑一次 `forge-agent new` → `generate` → 运行 agent 的流程，验证整个工具链可工作。

---

## 执行步骤

```bash
# 1. 跑冒烟脚本（自动）
cd /Users/popmart/Documents/python/forge-agent
bash scripts/smoke_test.sh

# 2. 或手动跑
cd /tmp
PYTHONPATH=/Users/popmart/Documents/python/forge-agent/src \
  python -m forge_agent.cli new my_app --template=basic
cd my_app
pip install -e /Users/popmart/Documents/python/forge-agent
PYTHONPATH=/Users/popmart/Documents/python/forge-agent/src \
  forge-agent llm list
PYTHONPATH=/Users/popmart/Documents/python/forge-agent/src \
  forge-agent generate "echo agent that returns hello" --provider=mock

# 3. 跑生成的 agent
cat > test_run.py <<'PYEOF'
import asyncio
from forge_agent import AgentContext
from agents.example import ExampleAgent

async def main():
    agent = ExampleAgent()
    await agent.initialize()
    report = await agent.run(AgentContext(scope_id="t1", scope_name="test"))
    print(f"Verdict: {report.verdict}")
    print(f"Run ID: {report.run_id}")
    assert report.verdict is not None
    assert report.run_id
    await agent.shutdown()

asyncio.run(main())
PYEOF
python test_run.py
```

---

## 验证标准

- [ ] `forge-agent new my_app` 成功创建项目
- [ ] `forge-agent llm list` 显示 5 个 providers
- [ ] `forge-agent generate` 生成代码（落盘到 `generated_agents/`）
- [ ] 生成的 agent 能 `import` + 实例化 + `run`
- [ ] 终端输出 `Verdict: <something>, Run ID: ...`
- [ ] 无异常退出

---

## 自动化验证命令

```bash
bash scripts/smoke_test.sh
# 返回 0 = pass, 非 0 = fail
```

---

## 产出物

- `scripts/smoke_test.sh`（冒烟测试脚本）
- `tests/e2e/test_smoke.py`（pytest 形式的冒烟测试）

---

## 状态记录

执行完成后，在本文件底部追加：

```markdown
## 执行记录

- **2026-06-27 12:00** ✅ 已完成
  - 创建 `scripts/smoke_test.sh`
  - 跑通 `forge-agent new → llm list → generate → run`
  - 生成的 agent 输出: `Verdict: Verdict.NEUTRAL, Run ID: run_xxx`
```

---

## 完成后

- [ ] 把 `⬜` 改成 `✅` 在 phase-0-overview.md
- [ ] **Phase 0 全部完成** → 停下来 review → 进入 Phase 1
