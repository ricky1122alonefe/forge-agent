# T2.1.1 — AgentType 枚举

| 字段 | 值 |
|---|---|
| **任务 ID** | T2.1.1 |
| **所属 Phase** | Phase 2 |
| **估时** | 0.5 天 |
| **依赖** | Phase 1 完成 |
| **必须性** | 🔴 核心 |
| **状态** | ⬜ 未开始 |
| **自动化验证** | `pytest tests/unit/test_agent_type.py` |
| **最近验证** | — |

---

## 目标

定义 Agent 类型枚举，支持按类型分类生成不同风格的 Agent：

1. **AgentType 枚举** — 定义 5 种核心类型
2. **类型描述** — 每种类型有清晰的描述和用途
3. **向后兼容** — 默认类型为 `GENERAL`

---

## 执行步骤

### Step 1: 创建 `src/forge_agent/core/agent_type.py`

```python
from enum import Enum

class AgentType(str, Enum):
    """Agent 类型枚举 — 决定生成代码的风格和结构。"""

    SCRAPER = "scraper"           # 数据抓取类
    ANALYZER = "analyzer"         # 数据分析类
    MONITOR = "monitor"           # 监控告警类
    GENERATOR = "generator"       # 内容生成类
    GENERAL = "general"           # 通用类（默认）

    @property
    def description(self) -> str:
        """返回类型的中文描述。"""
        descriptions = {
            "scraper": "数据抓取类 — 从网页/API 获取结构化数据",
            "analyzer": "数据分析类 — 处理数据并生成洞察",
            "monitor": "监控告警类 — 持续监控并在异常时告警",
            "generator": "内容生成类 — 生成文本/代码/报告",
            "general": "通用类 — 灵活的通用 Agent",
        }
        return descriptions.get(self.value, "未知类型")

    @property
    def use_cases(self) -> list[str]:
        """返回该类型的典型用例。"""
        cases = {
            "scraper": ["爬取商品价格", "抓取新闻标题", "提取 API 数据"],
            "analyzer": ["股票趋势分析", "用户行为分析", "情感分析"],
            "monitor": ["服务器监控", "价格波动监控", "系统健康检查"],
            "generator": ["生成周报", "代码生成", "内容创作"],
            "general": ["自定义任务", "多步骤工作流", "集成多个能力"],
        }
        return cases.get(self.value, [])
```

### Step 2: 写测试 `tests/unit/test_agent_type.py`

覆盖：
- ✅ 枚举值正确（5 种类型）
- ✅ description 属性返回中文描述
- ✅ use_cases 属性返回用例列表
- ✅ 默认类型为 GENERAL
- ✅ 字符串序列化（JSON 友好）

### Step 3: 验证

```bash
pytest tests/unit/test_agent_type.py -v
```

---

## 验证标准

- [ ] AgentType 枚举包含 5 种类型
- [ ] 每种类型有 description 和 use_cases
- [ ] 默认类型为 GENERAL
- [ ] 测试全部通过

---

## 自动化验证命令

```bash
pytest tests/unit/test_agent_type.py -v
```

---

## 状态记录

执行完成后追加：

```markdown
## 执行记录

- **2026-XX-XX HH:MM** ✅ 已完成
  - 创建 `src/forge_agent/core/agent_type.py`
  - 定义 5 种 AgentType
  - 新增 N 个测试，全过
  - git commit: `feat(core): add AgentType enum for agent classification`
```

---

## 完成后

- [ ] 把 `⬜` 改成 `✅` 在 STATUS.md
- [ ] IMPLEMENTATION_LOG.md 追加记录
- [ ] 下一个任务：T2.1.2 AgentRequirements 加字段
