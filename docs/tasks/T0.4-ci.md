# T0.4 — GitHub Actions CI

| 字段 | 值 |
|---|---|
| **任务 ID** | T0.4 |
| **所属 Phase** | Phase 0 |
| **估时** | 2 小时 |
| **依赖** | T0.1 |
| **必须性** | 🔴 阻塞 |
| **状态** | ⬜ 未开始 |
| **自动化验证** | `test -f .github/workflows/test.yml` |
| **最近验证** | — |

---

## 目标

创建 `.github/workflows/test.yml`，在 Python 3.10 / 3.11 / 3.12 三个版本上跑 ruff + mypy + pytest。

---

## 执行步骤

### Step 1: 创建 workflow 文件

```bash
cd /Users/popmart/Documents/python/forge-agent
mkdir -p .github/workflows

cat > .github/workflows/test.yml <<'EOF'
name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Lint with ruff
        run: |
          ruff check src tests
          ruff format --check src tests

      - name: Type-check with mypy
        run: mypy src

      - name: Test with pytest
        run: pytest -v --tb=short
EOF

git add .github/workflows/test.yml
git commit -m "ci: add GitHub Actions test workflow"
git push
```

### Step 2: 验证 CI 跑通

1. 打开 GitHub → Actions 页面
2. 确认 workflow 触发
3. 3 个 Python 版本矩阵全绿

---

## 验证标准

- [ ] `.github/workflows/test.yml` 存在
- [ ] 推送到 GitHub 后 CI 触发
- [ ] 3 个 Python 版本（3.10 / 3.11 / 3.12）全绿
- [ ] ruff 检查通过
- [ ] mypy 检查通过
- [ ] pytest 全绿

---

## 自动化验证命令

```bash
if [ -f .github/workflows/test.yml ] && grep -q "python-version" .github/workflows/test.yml; then
    echo "[T0.4] GitHub Actions CI           ✅ PASS"
else
    echo "[T0.4] GitHub Actions CI           ❌ FAIL"
    exit 1
fi
```

> 注：本地无法验证 CI 实际跑通，需推送到 GitHub 后查看 Actions 页面。

---

## 产出物

- `.github/workflows/test.yml`

---

## 状态记录

执行完成后，在本文件底部追加：

```markdown
## 执行记录

- **2026-06-27 11:00** ✅ 已完成
  - 创建 `.github/workflows/test.yml`
  - git commit: `ci: add GitHub Actions test workflow`
  - CI 状态: 3/3 Python 版本全绿（GitHub Actions URL: ...）
```

---

## 完成后

- [ ] 把 `⬜` 改成 `✅` 在 phase-0-overview.md
- [ ] T0.5（pre-commit）可并行
