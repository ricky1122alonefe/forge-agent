# T0.1 — Python 3.10+ 真环境

| 字段 | 值 |
|---|---|
| **任务 ID** | T0.1 |
| **所属 Phase** | Phase 0 |
| **估时** | 30 分钟 |
| **依赖** | 无 |
| **必须性** | 🔴 阻塞 |
| **状态** | ⬜ 未开始 |

---

## 目标

在本地或 CI 环境中安装 Python 3.10+（推荐 3.11），确保所有后续测试在正确的 Python 版本上跑。

---

## 为什么需要这个

- `pyproject.toml` 声明 `requires-python = ">=3.10"`，但当前环境是 Python 3.9.6
- 3.10+ 的特性（`X | None`、`match`、精确类型提示）已大量使用
- 所有 v0.3 之前的测试都在 3.9 跑过，**没真验证过 3.10+**

---

## 执行步骤

### 方案 A：pyenv（推荐）

```bash
# 1. 安装 pyenv
brew install pyenv

# 2. 安装 Python 3.11
pyenv install 3.11.7

# 3. 在项目目录设置本地版本
cd /Users/popmart/Documents/python/forge-agent
pyenv local 3.11.7

# 4. 验证
python --version  # 应显示 3.11.7
```

### 方案 B：Docker

```bash
# 1. 拉取镜像
docker pull python:3.11-slim

# 2. 进入容器
docker run -it --rm -v $(pwd):/app -w /app python:3.11-slim bash

# 3. 容器内验证
python --version
```

### 方案 C：brew 直接装

```bash
brew install python@3.11
python3.11 --version
```

---

## 验证标准

- [ ] `python --version` 显示 3.10+（推荐 3.11）
- [ ] 在项目根目录执行 `python -c "import sys; print(sys.version_info)"` 输出 `>= (3, 10)`
- [ ] `pip install -e ".[dev]"` 干净通过（无版本警告）
- [ ] `python -c "import forge_agent; print(forge_agent.__version__)"` 输出 `0.3.0`

---

## 产出物

- 本地 Python 3.10+ 环境就绪
- `.python-version` 文件（如果用 pyenv）

---

## 完成后

- [ ] 把 `⬜` 改成 `✅` 在 phase-0-overview.md
- [ ] 执行 T0.2（LICENSE）、T0.3（CHANGELOG）—— 可并行
